#Hutool可以提供完整的配置文件：
# 邮件服务器的SMTP地址，可选，默认为smtp.<发件人邮箱后缀>
host = smtp.126.com
# 邮件服务器的SMTP端口，可选，默认25
port = 25
# 发件人（必须正确，否则发送失败）
from = yangningkai@126.com
# 用户名，默认为发件人邮箱前缀
user = yangningkai
# 密码（注意，某些邮箱需要为SMTP服务单独设置授权码，详情查看相关帮助）
pass = ynk1063315004wy
